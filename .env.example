# Copy to .env and fill with your Twilio credentials
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
# Must be a WhatsApp-enabled Twilio number, prefixed with whatsapp:
TWILIO_WHATSAPP_FROM=whatsapp:+17343367101

# ===== SECURITY CONFIGURATION (Optional) =====
# These security features are opt-in and disabled by default.
# Enable them when you're ready to enhance security.

# Encryption Key (32 bytes, base64-encoded)
# Generate with: openssl rand -base64 32
# Enables AES-256-GCM encryption for sensitive files
# OPENCLAW_ENCRYPTION_KEY=

# HMAC Secret (for token-based authentication with expiration)
# Generate with: openssl rand -base64 32
# Enables HMAC-SHA256 tokens as an additional auth method
# OPENCLAW_HMAC_SECRET=

# Rate Limiting
# Uncomment to enable rate limiting (default: disabled)
# OPENCLAW_RATE_LIMIT_ENABLED=false
# OPENCLAW_RATE_LIMIT_PER_MINUTE=30

# Permission Level (1=READ_ONLY, 2=WRITE_SAFE, 3=EXECUTE_SAFE, 4=PRIVILEGED)
# Default: 4 (PRIVILEGED - no restrictions, like current behavior)
# OPENCLAW_PERMISSION_LEVEL=4
